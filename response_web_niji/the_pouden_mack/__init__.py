# 学习使用selenium插件做制动化测试案例，以及selenium的基本操作和调用方法

# Python爬虫环境常用库安装
# 调用注释中的文本


class Done(object):

        def dictoty(self):

            """
            @shaine
            #:except(15)

            :return:
            """
            pass
        try:

            if __name__ == '__main__':

                print(str(dictoty.__doc__))
            else:
                print("")

        except :

            print()

        def selenium_doc(self):
            """
            Python爬虫环境常用库安装

            1：urllib urllib.request

        这两个库是python自带的库，不需要重新安装，在python中输入如下代码：

        import urllib
        import urllib.request
        response=urllib.request.urlopen("http://www.baidu.com")
        print(response)
        1
        2
        3
        4
        返回结果为HTTPResponse的对象：
        <http.client.HTTPResponse object at 0x000001929C7525F8>

        2：正则表达式模块

        import re
        1
        该库为python自带的库，直接运行不报错，证明该库正确安装。

        3：request库的安装

        安装命令：pip install requests
        验证该库是否安装成功，运行如下代码：

        import requests
        response=requests.get("http://www.baidu.com")
        print(response)


        E:\JetBrains\TEST_Project\Test_selenium_response\phantomjs-2.1.1\phantomjs-2.1.1-windows\bin

            :return:
            """
            pass

